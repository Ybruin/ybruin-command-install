# ybruin-command-install

## Usage

    Usage: ybruin install <name> [options]

    Options:

      -h, --help         output usage information
      -f, --config       set components config file
      -r, --root <path>  set project root

